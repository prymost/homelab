# This is a sample file. Copy it to .env and fill in your actual secrets.
# The .env file is ignored by Git and should NEVER be committed.

export APP_PASSWORD="your-app-password"

# SMTP Credentials for Robusta/Alertmanager
export EMAIL_FROM="ross.mage@gmail.com"
export EMAIL_PASS="your-email-password-or-app-key"
export EMAIL_TO="polar-swipe-ranch@duck.com"
export SMTP_SERVER="smtp.gmail.com:587"
export SMTP_USER="ross.mage@gmail.com"
